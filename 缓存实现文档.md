# 库存管理模块Redis缓存实现文档

## 1. 设计思路

### 1.1 缓存目标
- 提升库存查询性能，降低数据库压力
- 确保缓存与数据库数据一致性
- 实现缓存降级方案，保证系统可靠性
- 提供监控和日志记录，便于问题排查

### 1.2 缓存策略
- **缓存键命名规范**：采用`inventory:{type}:{params}`的格式，确保缓存键的唯一性和可读性
- **过期策略**：根据数据类型设置不同的过期时间
  - 库存列表：5分钟
  - 库存详情：10分钟
  - 库存历史记录：3分钟
- **更新机制**：采用**写操作清除缓存**的策略，确保数据一致性

## 2. 实现细节

### 2.1 缓存工具类
- `RedisCacheUtil`：提供缓存的基本操作，包括设置、获取、删除缓存等
- `InventoryCacheKeyGenerator`：生成统一的缓存键，确保缓存键的一致性

### 2.2 缓存集成
- **库存列表查询**：缓存分页查询结果，提升列表页面加载速度
- **库存详情查询**：缓存单个库存详情，提升详情页面加载速度
- **库存历史记录查询**：缓存历史记录，提升历史查询性能
- **库存调整**：修改库存后清除相关缓存，确保数据一致性
- **安全库存设置**：修改安全库存后清除相关缓存，确保数据一致性

### 2.3 缓存降级方案
- 当Redis服务不可用时，自动切换到直接查询数据库模式
- 通过try-catch捕获Redis异常，确保业务流程不受影响

## 3. 代码结构

### 3.1 工具类
- `bysj.pets.bec.utils.RedisCacheUtil`：Redis缓存工具类
- `bysj.pets.bec.utils.InventoryCacheKeyGenerator`：库存缓存键生成器

### 3.2 服务实现
- `bysj.pets.bec.service.impl.InventoryServiceImpl`：集成Redis缓存的库存管理服务

### 3.3 性能测试
- `bysj.pets.bec.service.impl.InventoryServicePerformanceTest`：缓存性能测试类

## 4. 缓存键设计

### 4.1 库存列表缓存键
```
inventory:list:page_{pageNum}_size_{pageSize}_type_{productType}_status_{stockStatus}_keyword_{keyword}_seller_{sellerId}
```

### 4.2 库存详情缓存键
```
inventory:detail:{inventoryId}
```

### 4.3 库存历史记录缓存键
```
inventory:history:{inventoryId}_page_{pageNum}_size_{pageSize}
```

## 5. 缓存一致性保证

### 5.1 写操作清除缓存
- 库存调整后清除相关缓存
- 安全库存设置后清除相关缓存
- 确保缓存与数据库数据同步

### 5.2 过期策略
- 通过设置合理的过期时间，确保缓存数据最终一致性
- 避免缓存数据长期过期导致的不一致问题

## 6. 性能测试

### 6.1 测试方法
- 预热缓存：先执行多次查询，将数据加载到缓存
- 性能测试：执行100次查询，统计耗时
- 对比分析：比较缓存查询与直接数据库查询的性能差异

### 6.2 预期结果
- 库存列表查询响应时间降低至少50%
- 库存详情查询响应时间降低至少70%
- 库存历史记录查询响应时间降低至少60%

## 7. 维护指南

### 7.1 缓存监控
- 监控Redis服务状态
- 监控缓存命中率
- 监控缓存大小和内存使用情况

### 7.2 常见问题排查
- **缓存未命中**：检查缓存键生成是否正确，缓存是否过期
- **缓存不一致**：检查写操作是否正确清除了相关缓存
- **Redis连接失败**：检查Redis服务是否正常运行，网络连接是否畅通

### 7.3 优化建议
- 根据实际业务场景调整缓存过期时间
- 对于热点数据，可以适当延长缓存过期时间
- 对于频繁更新的数据，可以适当缩短缓存过期时间

## 8. 总结

本实现通过集成Redis缓存技术，显著提升了库存管理模块的查询性能，同时确保了缓存与数据库数据的一致性。缓存降级方案保证了系统的可靠性，即使在Redis服务不可用时也能正常运行。监控和日志记录便于问题排查和性能优化。

通过合理的缓存策略和实现，库存管理模块的查询响应时间预计将降低50%以上，为用户提供更流畅的使用体验。