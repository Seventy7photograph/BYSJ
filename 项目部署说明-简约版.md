# 摄影器材交易系统 - 部署说明

## 1. 环境要求

| 软件 | 版本 | 说明 |
| :--- | :--- | :--- |
| JDK | 17 | 后端运行环境 |
| Node.js | 20.x/22.x | 前端构建环境 |
| MySQL | 8.0+ | 数据库 |
| Redis | 7.x+ | 缓存 |
| Maven | 3.8+ | 后端构建 |

## 2. 部署步骤

### 2.1 数据库配置

```sql
-- 1. 创建数据库
CREATE DATABASE db_bysj_pets CHARACTER SET utf8mb4 COLLATE utf8mb4_bin;

-- 2. 导入数据
-- Windows:
mysql -u root -p123456 db_bysj_pets < "G:\study\AAA-BYSJ\数据库文件\db_bysj_pets.sql"

-- 3. 导入退款表
mysql -u root -p123456 db_bysj_pets < "G:\study\AAA-BYSJ\BYSJ.PETS.BEC\src\main\resources\refund.sql"
```

### 2.2 后端部署

```powershell
# 1. 进入后端目录
cd G:\study\AAA-BYSJ\BYSJ.PETS.BEC

# 2. 构建项目
mvn clean package -DskipTests

# 3. 运行服务
mvn spring-boot:run
# 或运行JAR包
java -jar target/BYSJ-PETS-BEC-0.0.1-SNAPSHOT.jar
```

### 2.3 前端部署

```powershell
# 1. 进入前端目录
cd G:\study\AAA-BYSJ\BYSJ.PETS.FEC

# 2. 安装依赖
npm install

# 3. 开发模式
npm run dev

# 4. 生产构建
npm run build
npm run preview -- --port 8080
```

## 3. 配置文件说明

### 后端配置 (application.yml)

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| server.port | 9090 | 后端端口 |
| spring.datasource.password | 123456 | 数据库密码 |
| spring.data.redis.port | 6379 | Redis端口 |
| file.upload.path | G:/study/AAA-BYSJ/... | 文件存储路径 |

### 前端配置 (.env)

```
VITE_APP_BASE_API=/api
```

## 4. 端口说明

| 服务 | 端口 |
| :--- | :--- |
| 前端开发 | 5173 |
| 前端预览 | 8080 |
| 后端服务 | 9090 |
| MySQL | 3306 |
| Redis | 6379 |

## 5. 验证部署

- 前端：http://localhost:5173
- Swagger文档：http://localhost:9090/swagger-ui.html

## 6. 常见问题

**Q: 数据库连接失败**
A: 检查MySQL服务是否启动，密码是否正确

**Q: Redis连接失败**
A: 检查Redis服务是否启动，默认无密码

**Q: 前端API请求失败**
A: 确认后端服务运行在9090端口，检查vite.config.ts代理配置

**Q: 端口被占用**
```powershell
netstat -ano | findstr :9090
taskkill /F /PID <进程ID>
```

## 7. 启动/停止脚本

**启动 (start.ps1)**
```powershell
net start MySQL80
Start-Process redis-server
cd G:\study\AAA-BYSJ\BYSJ.PETS.BEC; mvn spring-boot:run
cd G:\study\AAA-BYSJ\BYSJ.PETS.FEC; npm run dev
```

**停止 (stop.ps1)**
```powershell
Stop-Process -Name java -Force
Stop-Process -Name node -Force
net stop MySQL80
```

## 8. 安全注意事项

- 生产环境修改 `jwt.secret` 和 `sa-token.jwt-secret-key`
- 修改数据库默认密码
- 配置HTTPS（使用Nginx或IIS）
- 关闭Swagger UI（生产环境）
