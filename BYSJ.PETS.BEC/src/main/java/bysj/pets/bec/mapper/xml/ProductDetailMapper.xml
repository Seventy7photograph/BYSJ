<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bysj.pets.bec.mapper.ProductDetailMapper">

    <insert id="insert" parameterType="bysj.pets.bec.entity.ProductDetail">
        INSERT INTO product_detail (product_id, parameters, warranty_info, authorization, img_urls, description, usage_duration, repair_history, accessories, create_time, update_time)
        VALUES (#{productId}, #{parameters}, #{warrantyInfo}, #{authorization}, #{imgUrls}, #{description}, #{usageDuration}, #{repairHistory}, #{accessories}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectByProductId" parameterType="java.lang.Long" resultType="bysj.pets.bec.entity.ProductDetail">
        SELECT detail_id, product_id, parameters, warranty_info, authorization, img_urls, description, usage_duration, repair_history, accessories, create_time, update_time
        FROM product_detail
        WHERE product_id = #{productId}
    </select>
</mapper>
