<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bysj.pets.bec.mapper.InventoryHistoryMapper">

    <!-- 获取库存变更历史 -->
    <select id="selectInventoryHistory" resultType="bysj.pets.bec.entity.vo.InventoryHistoryVO">
        SELECT 
            id,
            change_time AS changeTime,
            change_type AS changeType,
            before_stock AS beforeStock,
            after_stock AS afterStock,
            change_quantity AS changeQuantity,
            reason,
            operator
        FROM inventory_history
        WHERE inventory_id = #{inventoryId}
        ORDER BY change_time DESC
    </select>
    
    <!-- 获取库存变更历史总数 -->
    <select id="selectInventoryHistoryTotal" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM inventory_history
        WHERE inventory_id = #{inventoryId}
    </select>
</mapper>
