<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bysj.pets.bec.mapper.OrderItemMapper">

    <!-- 批量插入订单项 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO order_item (order_id, product_id, quantity, unit_price, deposit, freight, item_status, create_time, update_time)
        VALUES
        <foreach collection="orderItems" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.quantity}, #{item.unitPrice}, #{item.deposit}, #{item.freight}, #{item.itemStatus}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <!-- 根据订单ID查询订单项 -->
    <select id="selectByOrderId" resultType="bysj.pets.bec.entity.OrderItem">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

</mapper>
